version: 2

sources:
  - name: bronze
    description: Bronze layer tables stored in the raid schema, prefixed with bronze_.
    database: workspace
    schema: raid

    tables:
      - name: bronze_champindex
        description: >
          Raw Champion Index snapshot data ingested via Auto Loader into
          workspace.raid.bronze_champindex.
